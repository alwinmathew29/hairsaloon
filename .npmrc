# Enable legacy peer deps to avoid resolution conflicts
legacy-peer-deps=true
# Include all optional dependencies by default
include=optional
# Audit level to avoid blocking builds
audit-level=moderate
